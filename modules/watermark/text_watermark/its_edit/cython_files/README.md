# README

Under this directory, use the following command to compile levenshtein.pyx:

```python
python setup.py build_ext --inplace
```

After running the above command, a compiled module file `levenshtein.cpython-310-x86_64-linux-gnu.so` will be generated and placed in the current directory, and then the levenshtein module can be imported successfully.

